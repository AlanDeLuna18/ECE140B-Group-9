# In Docker Compose, the backend reaches InfluxDB by the service name on the shared network.
INFLUX_URL=http://influxdb:8086

# Local development credentials created by the InfluxDB setup container.
INFLUX_TOKEN=plant-token
INFLUX_ORG=plant-org
INFLUX_BUCKET=plant-data
